package Main.logic;
public interface ArrayProcessor {
    double calculate(double[] oneDArr);
    void processArray(double[] oneDArr,double multip);
    double calculate(double[][] twoDArr);
    void processArray(double[][] array,double maxNum);
}